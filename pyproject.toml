[project]
name = "axiom"
version = "0.1.0"
description = "AXIOM — the company brain"
requires-python = ">=3.13"
dependencies = []

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
